# k8s Services

* [Debug Services](https://kubernetes.io/docs/tasks/debug-application-cluster/debug-service/)

Define services before pods if you want related environment variables to be defined in the pods (though you won't need that if your are using DNS) ([SO](https://stackoverflow.com/a/50524487/125246))